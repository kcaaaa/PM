# 全流程产品技能 · 使用场景介绍

## 该技能应该在何时使用

本技能是 AI 原生产品协作系统的总入口，适用于所有需要从自然语言想法推进到需求、设计、QA、实现、测试、反馈和变更的场景。

项目以 `Base/` 对象包为唯一项目认知基准，由 Orchestrator、PM Agent、QA Agent、Dev Agent、Domain Agent 与 BA / PM / Code / DB / UI / Council / Extension / Prompt 等 Skill 协同工作。使用者不需要懂代码、路径或技术细节，只需要描述业务想法、问题现象、使用感受和期望效果。

核心工作模式：

```text
人类输入方向
→ AI 识别意图与当前 Base 状态
→ AI 生成 3-5 个候选方案并推荐最优方案
→ 人类选择 / 修正 / 否定
→ AI 写入或更新 Base 对象包
→ PM 推进 RQ → VR → PD → TD → FT
→ QA Agent 执行强制验证
→ QA 通过后创建 IMP 并进入 Dev 实现
→ TEST 测试验收
→ FB 反馈 / CR 变更 / 回归测试
```

---

## 一、项目启动与 Base 初始化

当用户想要从零开始一个新产品、新业务、新工具或新代码项目时，应使用本技能建立项目认知基准。

**典型触发语句：**

- “我想做一个 XXX 产品，怎么开始？”
- “帮我初始化一个新项目。”
- “这个项目该怎么组织？”
- “先帮我搭一套需求和实现管理结构。”

**执行内容：**

- Orchestrator 判断是否缺少项目目标、需求基线或项目结构基线。
- BA / PM 生成 3-5 个产品方向或初始化方案，并推荐最优方案。
- 人类确认后，PM 创建或更新 `Base/00-项目总览/` 与首个 `RQ-*`。
- 明确 `Base/` 与真实代码工程目录的边界。
- 后续按 `RQ → VR → PD → TD → FT → QA → IMP → TEST` 链路推进。

---

## 二、模糊想法与业务分析

当用户只有初步想法、表达“说不清”“感觉不对”“不知道该做什么”时，应优先使用 BA Skill 帮助挖掘真实问题。

**典型触发语句：**

- “我有个想法，但不知道怎么落地。”
- “这个产品感觉不对，你帮我分析一下。”
- “帮我把想法变成方案。”
- “我要做需求调研 / 访谈问卷。”

**执行内容：**

- BA 判断用户当前缺的是方向判断、痛点推断、访谈设计、需求梳理还是优先级排序。
- 必要时使用 Council Advisor 视角辅助复杂判断。
- 输出 2-4 个业务理解或需求方案，并推荐一个默认方向。
- 人类确认后，PM 将确认内容落盘为 `Base/01-需求管理/RQ-*`。

---

## 三、需求管理与功能定义

当用户提出新需求、新功能、需求细化或需求边界调整时，应使用 PM 链路管理 Base 对象包。

**典型触发语句：**

- “我想加一个 XXX 功能。”
- “这个需求该怎么实现？”
- “帮我梳理一下当前需求。”
- “这个功能边界帮我定一下。”

**执行内容：**

- Orchestrator 扫描 `Base/` 中是否已有相关 `RQ-* / FT-*`。
- PM 输出 3-5 个候选需求或功能方案，评估人类意图匹配度、用户价值、实现复杂度、代码开发量、代码适配度、风险与扩展性。
- 人类确认后，创建或更新 `RQ-*`、`VR-*`、`PD-*`、`TD-*`、`FT-*`。
- 明确功能说明、功能边界、异常场景和验收标准。

---

## 四、产品设计与技术方案评估

当用户需要设计页面流程、用户路径、交互规则、接口、数据结构、安全策略或技术方案时，应使用 PM + Domain 组合。

**典型触发语句：**

- “帮我设计一下这个功能。”
- “这个页面流程该怎么走？”
- “技术方案怎么选？”
- “数据库表怎么设计？”
- “接口怎么定义比较合理？”

**执行内容：**

- PM 确认是否已有上级 `RQ-*` 与 `PD-*`。
- Domain 根据任务类型加载 `DB-skill`、`UI-skill` 或 Code 架构/API 子技能。
- 输出 2-4 个或 3-5 个候选方案，并说明适用场景、复杂度、开发量、适配度和风险。
- 人类确认后写入 `PD-*` 或 `TD-*`，并保持与 `FT-*` 验收标准一致。

---

## 五、QA 验证与实现前审查

当 `FT-*` 已确认，准备进入实现阶段时，必须使用 QA Agent。QA 是当前系统中位于功能定义和实现之间的强制环节。

**典型触发语句：**

- “这个功能确认了，准备进入实现。”
- “实现前帮我审查一下方案。”
- “这个方案风险大不大？”
- “帮我模拟真实用户走一遍。”

**执行内容：**

- QA Agent 读取相关 `RQ / VR / PD / TD / FT`。
- 执行五阶段验证：文档解析、用户模拟、风险评估、运营分析、报告生成。
- 输出 `Base/10-QA验证/QA-*`，包含 `QA报告.md`、`用户模拟记录.md`、`风险评估矩阵.md`、`运营分析.md`、`人类确认记录.md`。
- 结论分为通过、有条件通过、不通过。
- 人类确认通过后，PM 才能创建 `IMP-*` 并调度 Dev Agent。

---

## 六、功能实现与编码执行

当需求、设计、技术方案、功能定义和 QA 验证都完成后，应使用 Dev Agent 基于 `IMP-*` 执行代码任务。

**典型触发语句：**

- “开始实现这个功能。”
- “帮我写一下这个接口。”
- “前端页面按刚才方案做。”
- “把这个 Bug 修一下。”

**执行内容：**

- Dev Agent 读取 `IMP-*`，声明当前对应的 `RQ / FT / QA / IMP`。
- 按需加载 `Code-skill` 的 21 个子技能，以及 `DB-skill`、`UI-skill`。
- 自行扫描真实代码工程目录并定位文件，不要求非开发者提供路径。
- 修改真实代码工程文件，不把源码写入 `Base/`。
- 完成后更新 `IMP-*` 实现完成记录与 `TEST-*` 测试验收包。

---

## 七、Bug 修复与测试验收

当已有功能出现异常、测试失败、页面不符合预期或用户描述“哪里不对”时，应使用 TEST / BUG 修复链路。

**典型触发语句：**

- “这里有个 Bug，帮我修一下。”
- “测试报错了。”
- “点击保存后没有反应。”
- “这个页面打开很慢。”
- “帮我写测试用例。”

**执行内容：**

- PM / Dev 判断是否已有对应 `FT-* / IMP-* / TEST-*`。
- 如涉及产品意图偏差，先创建 `FB-*` 或 `CR-*`。
- Dev 基于 Code 调试、验证、性能、安全等子技能定位和修复问题。
- 将测试用例、测试记录、问题清单、验收结论写入 `Base/09-测试验收/TEST-*`。

---

## 八、反馈与变更处理

当实现结果与预期不一致，或已有功能需要调整时，应使用反馈与变更链路，不能直接覆盖原需求。

**典型触发语句：**

- “这里和我想的不一样。”
- “我想改一下之前做的功能。”
- “这个逻辑需要调整。”
- “能不能换成另一种方式？”

**执行内容：**

- 创建或更新 `Base/07-反馈管理/FB-*` 记录偏差。
- 创建 `Base/08-变更管理/CR-*`，包含变更说明、影响分析、AI 处理建议、人类决策记录、变更执行记录。
- 重大变更或高风险变更必须重新执行 QA。
- 人类确认 CR 后，进入增量 `IMP-*` 和回归 `TEST-*`。

---

## 九、专业领域咨询

当用户只需要数据库、UI、代码、架构、API、性能、安全、CI/CD、移动端、TypeScript 等专业建议，且不改变产品意图时，可直接路由 Domain 或 Dev 相关 Skill。

**典型触发语句：**

- “这个数据库结构合理吗？”
- “这个页面怎么更好用？”
- “API 该怎么设计？”
- “这个系统架构怎么选？”
- “性能瓶颈可能在哪里？”

**执行内容：**

- 判断是否影响产品目标、功能边界、数据结构或核心交互。
- 不影响产品意图时，可直接给专业建议。
- 影响产品意图或实现边界时，必须绑定 `TD-*` 或 `CR-*`，并给候选方案与推荐。

---

## 十、多 Agent 协作调度

当任务涉及产品、QA、代码、数据库、UI、测试、运维等多个领域时，本技能作为总入口统一调度。

**典型触发语句：**

- “这个功能涉及前端和后端，一起做吧。”
- “帮我从产品到实现完整做一遍。”
- “设计数据库、页面和接口方案。”
- “从需求到测试验收都帮我管起来。”

**调度规则：**

| 场景 | 调度对象 |
|---|---|
| 意图识别、方案推荐、链路判断 | `agents/orchestrator/SKILL.md` |
| 需求、设计、方案、功能、变更、测试管理 | `agents/pm-agent/SKILL.md` + `skills/pm/PM-skill.md` |
| 模糊需求、业务分析、访谈与痛点推断 | `skills/BA/BA-skill.md` |
| FT 确认后的实现前验证 | `agents/qa-agent/SKILL.md` |
| 编码、修复、调试、验证、架构、CI/CD | `agents/dev-agent/SKILL.md` + `skills/code/code-skill.md` |
| DB / UI / 技术方案专业判断 | `agents/domain-agent/SKILL.md` + `skills/db/DB-skill.md` / `skills/ui/UI-skill.md` |
| 通信、异常、跨 Agent 协作 | `protocols/communication-protocol.md` / `protocols/error-handling-protocol.md` |

---

## 核心判断标准

只要用户意图涉及以下任一维度，即应使用本技能：

1. **产品意图层面**：新需求、功能变更、页面流程、用户路径、功能边界。
2. **业务分析层面**：模糊想法、痛点推断、需求调研、访谈问卷、方案比较。
3. **实现执行层面**：编码、Bug 修复、测试、性能优化、接口与数据库实现。
4. **质量验证层面**：QA 审查、用户模拟、风险评估、运营分析、验收判断。
5. **项目管理层面**：Base 初始化、对象包维护、进度追踪、变更追溯。

## 使用禁区

- 不得在缺少上级 `RQ-*` 时直接创建 `FT-*` 或 `IMP-*`。
- 不得在缺少 `PD-* / TD-* / FT-*` 时直接进入实现。
- 不得跳过 `QA-*` 直接从 `FT-*` 进入 `IMP-*`。
- 不得把真实源码写入 `Base/`。
- 不得把需求变更当作普通代码优化处理。
- 不得替人类决定会改变产品目标、功能范围、核心流程或数据结构的事项。
